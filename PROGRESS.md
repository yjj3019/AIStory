# PROGRESS.md

프로젝트: **인공지능을 만든 사람들** (구 「빛이 된 아이 — 인공지능 70년 이야기」)
인물 중심 저널리즘 톤 인터랙티브 페이지(`AIStory.html`) + OmniVoice 내레이션

## 현재 상태

- 마지막 작업(2026-09-11): 사용자 요청으로 **동화형 서사(`빛이된아이.html`)와 팩트차트(`AI족보.html`) 두 파일을 `AIStory.html` 하나로 병합·재작성**. 톤을 동화에서 인물 전기/저널리즘으로 전환하고, 낭독 분량을 30~40분(10,046자)으로 확장. 원본 두 파일은 삭제됨(버전관리 미추적 상태였으므로 복구는 세션 기록/백업에 의존)
- 진행 상태: 콘텐츠 재작성 + 음성 재렌더링까지 **전 과정 완료**. `rhel-storage:/home/jjyoo/record`(CPU, GPU 없음, venv, root 권한 없음)에서 사용자가 직접 새 `narration.json`(10,046자) 기준 전체 17클립 재렌더링, `audio/`에 wav+mp3+manifest.json 전량 교체 완료(2026-09-11 15:57). RTF 14.6~19.4(10코어 제한), 총 소요 약 2시간 25분
- **경로 정정**: README.md는 `tts/` 하위 디렉터리를 전제로 쓰였지만 `extract_narration.py`, `render_tts.py`, `narration.json`, `Containerfile`은 모두 이 디렉터리 **루트**에 있다. 명령 실행 시 `tts/` 접두사를 붙이지 말 것 (CLAUDE.md에도 명시됨)
- **(2026-09-15) 16번째 장 "가계도 밖에서" 추가 + 정확성 수정**: 기존 15장(다트머스→OpenAI/Anthropic 계보) 구조는 전혀 건드리지 않고, 그 뒤에 NVIDIA·메타·구글 딥마인드·OpenAI·Anthropic·DeepSeek·Mistral의 현재(2026-09) 리더십 8명을 다루는 신규 장을 append. 데이터는 P/ORG와 분리된 `RIVALS` 상수로 두어 SVG 가계도 좌표·엣지 체계는 무변경. 하사비스 카드 설명을 2026-08 구글 딥마인드 리더십 개편(회장·Alphabet Chief Scientist로 전환, 실무는 카부쿠오글루) 반영해 정정. `narration.json` 재추출 결과 기존 00~15 항목은 바이트 단위로 불변, 신규 `16-scene` 1개만 렌더 대상(`--dry-run` 확인 완료). 엔딩 id는 `16-ending`→`17-ending`으로 변경(신규 16장과의 id 충돌 회피) — 텍스트 불변이라 오디오 파일은 재합성 없이 이름만 변경(`audio/16-ending.*`→`17-ending.*`). **아직 안 한 것: 신규 `16-scene` 클립 1개의 실제 음성 합성**(로컬 PC에 torch/omnivoice는 설치돼 있으나 voice_prompt 캐시가 rhel-storage에만 있어 그쪽에서 증분 렌더 권장) + 브라우저 실사용 검증(Chrome 확장 미연결로 이번 세션에선 HTTP/Node 레벨 검증까지만 완료).

## 완료

- 발표용 PPT 20장 (사람 계보 중심, S-Core 브랜드 아닌 자체 디자인) | `AI_족보학.pptx`
- 동화형 인터랙티브 웹페이지 15장면 + 인터랙티브 인물 지도(13장) | `빛이된아이.html`
- 팩트차트형 인물 계보 아티클 (신문 톤, 클릭 가능한 SVG 가계도) | `AI족보.html`
- 내레이션 대본 (장별 낭독시간·화면지시·실제사건 대조표) | `내레이션_대본.md`
- 내레이션 추출 스크립트 (HTML → JSON, Windows 콘솔 인코딩 버그 수정) | `extract_narration.py`
- OmniVoice 렌더링 스크립트 (Windows 콘솔 인코딩 버그 수정) | `render_tts.py`
- 추출 결과 17개 항목 1,913자 | `narration.json`
- 설치·운영 가이드 (dnf/venv/podman/영상 타임코드) | `README.md`
- GPU 컨테이너 정의 | `Containerfile`
- 참조 음성 기반 voice cloning으로 전체 17개 클립 렌더링 (RTF 12.4~22.5, 총 소요 약 72분) | `audio/`
- 두 콘텐츠를 인물 중심 저널리즘 톤 단일 페이지로 병합·재작성, 30~40분 분량으로 확장, 적대적 리뷰 1회전 반영 | `AIStory.html`

검증한 것:
- 추출 스크립트 재실행 성공(narration.json 내용 불변 확인), 렌더러 `--dry-run` 성공(Windows 콘솔 en-dash 크래시 수정 후)
- 이 PC 환경에서 `torch`(CPU 빌드)·`omnivoice`는 이미 설치돼 있고 `huggingface.co` 접속도 정상이다. GPU는 없음(Intel 내장 그래픽만) → `--device cpu` 필수, 느릴 수 있음.
  - **[정정]** 이전에 "이 PC 네트워크가 200~300 B/s로 막혀 있다"고 기록했었으나, 이는 **Claude Code 에이전트 세션(Bash 도구) 자체의 네트워크 샌드박스 제약**이었던 것으로 확인됨 — 사용자가 실제 PowerShell에서 `Invoke-WebRequest`로 같은 파일(2,238바이트)을 받았을 때 0.59초로 정상 완료됐다. **사용자 PC의 실제 네트워크는 문제 없음.** 에이전트 세션에서 직접 `curl`/`Invoke-WebRequest`로 대용량 파일을 내려받아야 하는 작업은 이 제약을 감안해야 하지만, 사용자가 터미널에서 직접 실행하는 `render_tts.py`는 영향받지 않는다.
- `AI족보.html`의 2026년 최신 인물 이동 서술을 웹 검색으로 검증: 노암 셰이저 OpenAI 복귀·존 점퍼 Anthropic 이적·미라 무라티 TML 이탈은 사실 확인. **OpenAI/Anthropic 기업가치 수치가 뒤바뀌어 있던 오류**(9,650억 달러는 Anthropic, OpenAI는 852억 달러) 발견 후 정정. 메타 "9개월 만에 권한 축소" 서술은 메타의 공식 부인이 있어 단정 표현을 완화
- `AI족보.html` 접근성 보강(`aria-live`, `aria-pressed`) 및 코드 정리(중복 CSS, 죽은 분기, 초상화 폴백 헬퍼 통합) 완료
- `빛이된아이.html` 오프닝/엔딩 오디오 미재생 버그 수정 — 기존에는 `00-opening.mp3`/`16-ending.mp3`를 만들어도 재생 로직이 15개 장면만 순회해 재생되지 않았음

- 전체 17개 클립 CPU 렌더링 실측: 약 72분(20:03:55~21:15:24), RTF 12.4~22.5. 음질은 사용자 직접 청취로 "실제 목소리와 거의 동일"하다고 판정(단, 참조 음성 자체에 경미한 하울링 있음 — 필요시 재녹음 검토 대상)

검증하지 못한 것: 고유명사 발음 자연스러움, 17개 클립 전체의 목소리 톤 일관성(청취 전수 확인 아직 안 함)

## 다음 우선순위

1. 발음·속도 튜닝
   - Acceptance Criteria:
     - 고유명사(오픈에이아이, 앤트로픽, 트랜스포머 등) 어색한 발음 없음 — 17개 클립 전수 청취 필요
     - 장면 간 목소리 톤 일관
     - 수정은 `AIStory.html` 원문 → `extract_narration.py` 재실행 경로로만 진행

2. 웹페이지 브라우저 검증
   - Acceptance Criteria:
     - 15장 스크롤 내비게이션, 인물 카드 클릭/키보드 상호작용, 계보도 SVG 렌더링 정상 동작
     - `audio/` 새 클립이 실제로 재생되는지(재생 바 "고품질 음성" 표시), 콘솔 에러 없음

3. 영상 제작
   - Acceptance Criteria:
     - `manifest.json` 기준 장면 전환 타이밍 산출
     - 웹페이지 화면 녹화 + 음성 합성
     - 자막 필요 시 `--granularity line`으로 문장 단위 재렌더링

## 결정 사항

- 결정: `빛이된아이.html`(서사)과 `AI족보.html`(팩트차트)을 **하나로 재작성하지 않고 상호 보완 관계로 유지**
  - 이유: 두 톤이 의도적으로 다르고(동화 vs 신문), 전체 재작성은 검증된 구조·접근성 패턴을 버리는 과잉 작업
  - 대안: 완전 통합 단일 파일
  - 위험: 두 파일 간 사실 정합성을 계속 함께 관리해야 함 — 이번에 첫 교차검증·정정 완료
  - **[Codex 리뷰 대상 아님 — 사용자 승인 하 직접 진행]**

- 결정: `AI족보.html`의 인물 관계망(`P`/`ORG`/`E` 데이터, 접근성 패턴)을 `빛이된아이.html` 13장에 축약 이식해 클릭형 "인물 지도"로 확장
  - 이유: 서사 몰입과 사실 정보량을 동시에 만족시키기 위해 서사 쪽에 최소한의 인터랙션을 추가
  - 대안: 전체 가계도를 그대로 이식(과함), 링크만 걸기(정보량 부족)
  - 위험: 없음. 기존 SVG/CSS 애니메이션 언어(draw/pop/glow)와 접근성 패턴(role=button, keydown)만 재사용, 외부 라이브러리 미도입

- 결정: TTS를 실시간 합성이 아닌 **사전 렌더링** 방식으로 구현
  - 이유: OmniVoice는 PyTorch 기반이라 브라우저에서 직접 실행 불가.
    내레이션은 고정 텍스트 17개뿐이고 최종 목표가 영상이라 파일이 그대로 재사용됨
  - 대안: FastAPI 추론 서버 / sherpa-onnx WASM 브라우저 실행
  - 위험: 문구 수정 시 재렌더링 필요. `extract_narration.py`로 동기화 경로 확보함

- 결정: **voice cloning 필수**, `create_voice_clone_prompt()` 결과를 캐시해 전 장면 재사용
  - 이유: 공식 tips.md 상 참조 음성 없이는 짧은 클립이 불안정.
    장면당 5~10초라 해당됨. auto 모드는 장면마다 목소리가 달라짐
  - 대안: `--instruct` voice design 모드 (참조 음성 없을 때) — 이번 1차 렌더링은 이 모드로 진행
  - 위험: 참조 음성 품질이 결과를 좌우함

- 결정: HTML을 문구의 **유일한 원본**으로 삼음 (단, 오프닝/엔딩 문구는 예외)
  - 이유: 대본·음성·화면이 어긋나는 것을 구조적으로 차단
  - 대안: narration.json을 원본으로 두고 HTML 생성
  - 위험: `extract_narration.py`는 SCENES 배열 파싱에 의존하므로 HTML의 `year:`/`lines:[` 구조를 바꾸면 스크립트도 함께 고쳐야 함. 오프닝(`OPENING`)/엔딩(`ENDING`) 문구는 HTML이 아니라 `extract_narration.py` 자체의 상수에 있음 — 고치려면 스크립트를 직접 수정. `빛이된아이.html`의 `ENDING_TEXT`는 `#closing .quote` DOM에서 읽어와 중복을 피했지만, `OPENING_TEXT`는 HTML에 하드코딩 중복돼 있어 `extract_narration.py`의 `OPENING`과 수동 동기화 필요

- 결정: 웹페이지는 오디오 파일이 없으면 브라우저 내장 음성으로 자동 폴백
  - 이유: 렌더링 전에도 페이지가 온전히 동작해야 함
  - 대안: 오디오 필수화
  - 위험: 없음

- 결정: PPT·웹페이지 모두 S-Core 브랜드 서식 미적용
  - 이유: 사내 산출물이 아닌 개인 콘텐츠. 주제에 맞춘 자체 디자인 채택
  - 대안: score-docs 스킬 적용
  - 위험: 사내 공유 목적이 생기면 재작업 필요

- 결정: MeloTTS(CPU 전용, myshell-ai) 대안 검토 후 **폐기, OmniVoice 경로 유지**
  - 이유: (1) voice cloning 미지원 — KR 고정 화자 1종뿐, (2) 실제 청취 결과 "너무 로봇 같은 음성"(사용자 판정)으로 음질 기준 미달. CPU 속도는 우수했음(RTF 약 0.65~1.0, 문장당 4~7초, OmniVoice보다 빠름)이나 voice cloning·음질이라는 주 기준에서 탈락
  - 대안: MeloTTS + OpenVoice tone-color converter 결합(미검증, 추가 파이프라인 복잡도 발생) — 이번엔 시도하지 않음
  - 위험: 없음(격리된 venv에서만 검증, 프로젝트 파이프라인/렌더링 스크립트 미변경)
  - 부가 발견: 설치 시 문서화 안 된 런타임 의존성 3개(pkg_resources/setuptools<81, eunjeon, unidic 526MB) 수동 해결 필요했고, HuggingFace Xet CDN(`us.aws.cdn.hf.co`)이 이 네트워크에서 전면 차단되어 체크포인트·BERT 가중치를 브라우저로 수동 다운로드해야 했음 — 향후 유사한 HF 기반 패키지 검토 시 같은 네트워크 제약을 먼저 예상할 것
- 결정: **OmniVoice CPU 렌더링 경로 최종 확정(GO)** — "CPU라 느려서 못 쓴다"던 기존 우려는 근거 부족으로 판명, 기각
  - 이유: 로컬 모델 경로(`--model`)로 Xet CDN 차단을 완전 우회해 실측한 결과 RTF=14.676(오프닝 클립, 10.0초 오디오에 146.3초). narration.json 전체 1,913자 기준 총 오디오 약 4.4분 → **전체 17개 클립 CPU 렌더링 예상 약 65분**, 1회성 오프라인 배치로 충분히 실용적
  - 음질: 생성된 `00-opening.wav`를 사용자가 직접 청취, "MeloTTS보다 훨씬 좋다"고 판정 — voice cloning 미지원으로 폐기된 MeloTTS와 달리 이 경로가 유일하게 음질·화자 지정 요구사항을 모두 만족
  - 발견한 버그: README/이 문서에 있던 예시 명령 `--instruct "female, warm, low pitch, gentle"`이 실제로는 **무효한 명령**이었음(현재 omnivoice 버전은 고정 키워드만 허용, `warm`/`gentle` 미지원 → ValueError). `--instruct "female, low pitch"`처럼 지원 키워드만 조합해야 함. 문서 수정 필요(다음 우선순위에 반영)
  - 테스트 산출물: `AIStory/melotts-test/`(MeloTTS venv+클론, OmniVoice 로컬 모델 사본 `omnivoice-local/` 포함, 총 수GB), `AIStory/checkpoint.pth`, `AIStory/config.json`, `AIStory/vocab.txt`, `AIStory/bert-kor-base/` — 전부 `.gitignore` 처리됨. `omnivoice-local/`은 실제 전체 렌더링 시 재사용 가능(재다운로드 불필요), 나머지는 필요 없으면 삭제 가능

- 결정: `빛이된아이.html`(동화 서사)과 `AI족보.html`(팩트차트)을 유지하던 기존 결정(위 "하나로 재작성하지 않고 상호 보완 관계로 유지")을 **뒤집고 `AIStory.html` 하나로 병합**
  - 이유: 사용자가 두 파일을 따로 가질 필요가 없다고 판단, "최고의 AI 전문가들의 업적과 회사 이력을 인물 중심으로 서술하는 AI 역사"를 원함. 이미 `AI족보.html`의 `P`/`ORG`/`E`/`CH` 데이터가 원하는 내용을 대부분 담고 있어 새로 쓰지 않고 두 자산(팩트차트의 콘텐츠 + 동화 페이지의 스크롤/오디오 셸)을 병합하는 방식으로 진행
  - 톤: 동화 → 인물 전기/저널리즘으로 전환(사용자 확정). 장면별 빛/아이 비유 SVG 삽화는 전량 삭제, 인물 카드·계보도가 시각적 중심으로 대체(사용자 확정)
  - 서브에이전트 활용: 초안 작성 1회 + 사용자 추가 요청으로 분량 확장(30~40분, 10,046자) 1회를 implementer 에이전트에게 위임, 별도 technical-reviewer 에이전트로 적대적 리뷰 1회전(사실 정합성·서사적 완결성·중복/공백 기준) 후 지적사항 중 근거 있는 7건만 채택해 직접 반영(3장 인물 미배치, 금액 병기 방식 등 2건은 근거 부족/기존 검증 사실이라 기각)
  - 위험: 기존 17클립 음성이 텍스트 전면 교체로 무효화됨 — 사용자가 별도 서버에서 재렌더링 예정(이번 세션 범위 아님). 원본 두 HTML은 버전관리 미추적 상태였고 이번에 삭제됨(복구 필요시 세션 기록 참고)

- 결정: 음성 재렌더링은 `rhel-storage:/home/jjyoo/record`에서 **컨테이너 없이 plain venv**로 진행(GPU 없는 CPU 서버)
  - 이유: 1회성 스크립트 실행에 컨테이너 빌드는 과함(사용자 판단). `Containerfile.cpu`는 만들어 프로젝트에 남겨뒀으나 이번엔 미사용
  - 과정에서 해결한 문제 3가지, 전부 render_tts.py/문서에 반영됨:
    1. `pip install torch`가 기본으로 CUDA 번들(수 GB)을 받아 디스크 부족 발생 — CPU 서버라 CPU wheel만 필요함을 확인 후 재설치로 해결
    2. huggingface_hub 접속 시 `SSL: CERTIFICATE_VERIFY_FAILED` — 사내망 자체 서명 CA 문제. `SSL_CERT_FILE`/`REQUESTS_CA_BUNDLE`/`CURL_CA_BUNDLE`을 시스템 CA 번들(`/etc/pki/tls/certs/ca-bundle.crt`)로 지정해 해결
    3. `ffmpeg-free`(dnf) 설치 불가(비-root, 레포 미설정) — `pip install imageio-ffmpeg`로 받은 정적 바이너리를 `venv/bin/ffmpeg`에 심볼릭 링크해서 render_tts.py가 그대로 찾게 함
  - `render_tts.py`에 `--cpu-cores N` 옵션 추가(기본값 없음 = 기존처럼 전체 코어 사용, 지정 시 sched_setaffinity + torch.set_num_threads + OMP/MKL 환경변수로 제한) — 공유 서버 자원 절약 목적, 사용자 요청으로 추가
  - `render_tts.py`에 tqdm 전체 진행률 표시줄 추가(장시간 렌더링 중 진행 상황이 안 보인다는 사용자 피드백 반영)
  - 실행 결과: `--cpu-cores 10`, 17클립(16개 신규 생성) 총 소요 2시간 25분, RTF 14.6~19.4. wav만 우선 생성 후(ffmpeg 없어 mp3 스킵) 위 3번 해결 후 mp3 17개 별도 변환, 로컬 `audio/`로 전량 교체 완료
  - 위험: 없음(재현 가능한 절차로 문서화 완료, 향후 재렌더링 시 동일 과정 반복 가능)

- 결정: 스크롤 텍스트 위주였던 구성에 **노선도(Metro map) 기반 인물 이동 시각화 + 인트로 시작 게이트** 추가(사용자 요청 — "영상처럼 역동적으로", "마지막에 지금 어디서 뭘 하는지")
  - 과정: 3개 에이전트(흐름도/성장형 계보도/노선도)가 병렬로 방향을 제안, 사용자가 노선도를 최종 선택(기존 재생바와 같은 하단 sticky 자리 재사용, 가장 직관적인 은유, 리스크 최저)
  - 구현: `STATIONS`(정류장 11개: ORG 7 + 학계/연구소 4) + `SCENES[i].at`(장별 소속 매핑) + `#metroWrap` SVG(rider가 CSS 변수+transform으로 정류장 간 이동) + 15장 진입 시 전체화면 확대·파도 도착 애니메이션 + `#intro` 시작 게이트. implementer 에이전트에게 위임 후 검증·정확도 수정 직접 반영
  - 발견·수정한 정확도 문제: 머스크가 최종적으로 OpenAI에 남은 것으로 표시되던 버그(xAI로 정정), 르쿤이 메타에 남은 것으로 표시되던 버그(정류장 부족 — `indie`(독립 스타트업) 정류장 신설 후 정정)
  - 알려진 한계: `STATIONS` 11개로는 Cohere/Character.AI/Essential AI 등 일부 소속이 표현 안 됨(11개 제약 내 근사). hinton/krizhevsky의 초기 "구글" 시절은 `deepmind` 정류장으로 근사 매핑
  - 위험: 없음(추가 레이어, 기존 `#tree`/오디오/자막 로직 무변경)

- 결정: 인물 사진을 **위키미디어 커먼즈 CC 라이선스만** 사용해 수집(구글 이미지 등 저작권 있는 사진 배제)
  - 이유: 저작권 위반 리스크 회피. 프로젝트가 공개될 가능성을 감안
  - 결과: 19명 중 13명 확보(`portraits/*.jpg`), 7명(krizhevsky/vaswani/shazeer/gomez/daniela/schulman/leike)은 커먼즈에 사진 자체가 없어 이니셜 폴백 유지 — 억지로 다른 출처 안 씀
  - CC BY/BY-SA 라이선스의 저작자 표시 의무를 지키기 위해 페이지 하단에 `#photo-credits` 섹션 신설, 13명 전원의 라이선스 종류 명기
  - 위험: `sutskever.jpg`는 2인 그룹샷을 크롭한 사진이라 프레이밍이 완벽하지 않을 수 있음 — 육안 확인 권장

- 결정: "여전히 텍스트만 읽는 느낌"이라는 사용자 피드백에 따라 **시각적 리듬 강화** 2차 작업 진행(대형 사진·풀블리드·노선도 플래시·시대적 맥락 이미지)
  - 장별 대형 인물 사진(`hero-por`, 200px): 1,2,4,7,9,11,13,14장에 portraits 보유 인물 최대 2명 배치
  - 핵심 전환점 3개 장(5·10·12장, AlexNet/ChatGPT/해임복귀)을 `.page.feature` 풀블리드로: 배경 사진+블러+그라디언트, 스탯 확대
  - 장 전환 시 노선도(`#metroWrap`) 상단 플래시: 이동이 있는 장에서 1.5초간 확대 오버레이 후 하단 sticky로 복귀
  - 시대적 맥락 이미지 5장(다트머스/빈티지 컴퓨터/GPU/ChatGPT시대/OpenAI 본사, 전부 CC, `context-images/`) 위키미디어에서 확보 — 용량 과다(13MB/10MB) 2개는 Pillow로 리사이즈(각 0.2~0.4MB로 축소). 그중 시각 공백이 명확했던 3장(겨울)·8장(트랜스포머, 인물 사진 없음)에만 배치 완료. 나머지 3장(dartmouth/chatgpt-launch/openai-hq)은 이미 사진이 있는 장과 중복돼 억지로 안 넣고 폴더에 보관 — 추후 필요시 배치 가능
  - 구현: implementer 에이전트가 격리된 worktree 사본에서 작업(원본 경로 Edit/Write 차단) → 완료 후 검증하여 실제 경로로 직접 복사·재검증
  - 위험: 없음(추가 레이어, 기존 구조 무변경). 사진 없는 인물(krizhevsky/vaswani/shazeer/gomez/daniela/schulman/leike)은 여전히 이니셜 폴백

- 결정: 사내 3라운드 리뷰(팩트체크/코드정합성/UX) 이후 **Codex 적대적 리뷰**를 추가 실행, 신규 결함 5건 전부 채택·수정
  - **[Codex 리뷰]** 모바일 마지막 장에서 노선도 배지 숫자와 실제 표시 인원 불일치(클라이맥스에서만 모바일 4인 제한 해제로 수정) / 확장된 긴 장에서 IntersectionObserver가 고정 임계값(.45)에 못 미쳐 배경색·노선도가 멈추는 문제(최다-가시 섹션 추적 방식으로 재작성) / 전역 화살표키가 인물노드·라이더 등 위젯 내부 키보드 탐색을 가로채는 문제(Space와 동일하게 body 포커스로 범위 제한) / 힌튼·크리제프스키가 노선도상 '딥마인드'로 표시되는데 본인 계보도 이력은 '구글'이라 모순(googlebrain으로 통일) / backdrop-filter에 -webkit- 접두사 누락(4곳 추가)
  - 위험: 없음(전부 버그 수정, 신규 기능 아님)

- 결정: Codex + Opus 독립 병렬 검토(사실/추가사항/디자인) 후 Fable(모델)에게 최종 채택/기각 판단 위임, 채택 11건 전부 반영
  - **사실 오류(Critical)**: OpenAI 기업가치 표기가 "852억 달러"로 돼 있었는데 실제는 10배인 "8,520억 달러"($852B)였음 — 이전 라운드(3차 팩트체크)가 "둘이 뒤바뀐 오류"로 잘못 정정했던 것. Opus가 Axios/CNBC 원문 대조로 자릿수 오류를 확정. Anthropic 9,650억 라운드·OpenAI S-1 제출 날짜도 "6월"→"5월"로 정정(둘 다 실제 5/22~28)
  - **내부 모순 해소**: TML 설립을 "퇴사 즉시"처럼 서술하던 것을 실제 시차(9월 퇴사→익년 2월 설립→7월 시드)로 분리, ORG/STATIONS 명칭을 "Thinking Machines Lab"으로 통일
  - **서사 보강**: 1장에 앨런 튜링(1950년 "기계는 생각할 수 있는가") 2문장 추가, 5장에 페이페이 리/이미지넷 데이터셋 제작자 언급 추가 — 둘 다 Codex·Opus 공통 지적
  - **노선도 정확도**: 힌튼(2023년 구글 퇴사를 반영해 독립 정류장으로), 슐먼·바스와니·고메즈·크리제프스키(15장 최종 상태를 각자 실제 이력에 맞게 갱신) — Codex가 잡은 힌튼 버그의 잔여분
  - **UX**: 인트로 "시작" 버튼이 계보도·hero 섹션을 건너뛰고 1장으로 직행하던 것을 자연 스크롤 동선(hero부터)으로 변경 — 가장 정보 밀도 높은 계보도가 기본 동선에서 빠져있던 문제
  - **디자인**: 풀블리드 3개 장(아바타용 저해상도 사진을 배경으로 확대 사용해 흐릿하게 보이던 문제)에 블러 처리를 더해 업스케일 티가 덜 나게 완화(원본 재촬영/재수집은 범위 밖이라 기각), 본문(`.lines`) 중앙정렬 → 좌측정렬(저널리즘 톤에 맞게, 제목/연도는 중앙 유지)
  - 기각 9건: 젠슨 황/NVIDIA, 딥시크 등 비서구 계보, 슈미트후버/LSTM, 초정렬 용어 각주, 셰이저 직함 재확인(근거 부족), SSI 현재형 시제(현재도 사실), hero-por 공백·표 폰트·feature 선정(취향 영역), scroll-padding 등 — Fable 판단 근거는 PROGRESS.md보다 세션 기록 참고
  - **위험**: 텍스트가 10,167자 → 10,470자로 또 늘어나 **기존 렌더링된 음성이 다시 무효화됨** — 재렌더링 필요

- 결정: 사용자 요청으로 "10회 시뮬레이션"(문장/AI표현/과장/디자인) 반복 검토 — fork 에이전트 3개에 각각 3~4패스 내부 반복 지시, 발견사항 채택 반영
  - **문장/AI표현**(4패스): 15개 장 전부가 "그런데 이것이 X를 의미한다" 식 메타-요약으로 끝나는 구조적 반복 발견(LLM 특유 습관) — 이전 서사 리뷰는 이를 "의도된 리듬"으로 긍정 평가했던 바 있어 이번엔 **전면 재작성은 보류**, 대신 가장 명백한 중복만 정정: 11장에서 같은 반전("안전 표방 조직이 최대 경쟁자가 됨")을 두 문장 연속으로 반복하던 부분 제거
  - **최근 수정분 재검증**(3회 교차): 튜링/OpenAI밸류/TML시점/8장창업비율/수츠케버·SSI날짜 전부 재확인 통과. 페이페이 리 소속 표기만 미세 조정("스탠퍼드의"→"당시 스탠퍼드의" — 그는 이미지넷을 프린스턴에서 시작해 2009년 스탠퍼드로 이직, 2012년 대회 시점 기준임을 명확히 함)
  - **디자인**(3회차): 인물별 고유색 이니셜 아바타 중 노란-연두 색조 구간(vaswani/gomez/daniela)이 `--paper` 글자와 WCAG AA(4.5:1) 대비 기준 미달 확인(실측 3.57~3.86) — 명도를 32%→25%로 낮춰 전체 색상환에서 최저 대비 5.22로 보정. hero-por-photo의 폴백 에러 핸들러가 사실상 도달 불가능한 죽은 코드라는 지적은 무해해서 기각(정리는 다음 리팩터링 때)
  - 위험: 텍스트 미세 수정(11장 문장 삭제, 5장 문구 1곳) — 재렌더링 대기 중인 음성과 또 다시 어긋남, 재렌더링 전 최종 diff 확인 권장
  - **사용자 확정**: 15개 장 전부가 메타-요약으로 끝나는 구조는 의도된 문체로 유지, 더 손대지 않음. 이 시점 텍스트를 최종본으로 확정 — 이후 재렌더링 대상

## Handoff

- 핵심 컨텍스트:
  - 콘텐츠는 1956 다트머스부터 2026년 현재까지 **인물 계보** 중심 저널리즘 서사. `AIStory.html` 하나뿐(2026-09-11 병합, 동화형 서사·팩트차트 원본 삭제됨)
  - 페이지 구성: 스크롤 기반 15장(`SCENES` 배열, 낭독용 `lines` 30~40분 분량) + 인물 계보도(`P`/`ORG`/`E`, 클릭 가능한 SVG 트리) + 전 장에 적용된 클릭형 인물 카드(`wireMap()`, `data-map`)
  - 파일명 규칙: `audio/NN-scene.mp3`(장면), `00-opening`, `16-ending`.
    HTML의 `clipName()`이 이 규칙에 의존. 오프닝/엔딩도 재생 로직에 연결됨(`speakText()`)
  - 외부 의존성 없음(구글 폰트 링크만). 단일 파일로 배포 가능
  - OmniVoice: Apache-2.0, 한국어 8,609시간 학습, `language="ko"`, GPU 기준 RTF 0.025(H100). 이 PC는 GPU 없어 `--device cpu` 필수

- 미결 사항:
  - 참조 음성(`ref/narrator.wav`)에 경미한 하울링 — 재녹음 여부는 선택 사항, 재녹음 시 전체 재렌더링 다시 필요
  - 17개 클립 고유명사 발음 전수 청취 미완료
  - 브라우저 실제 렌더링 미확인(스크롤/카드 클릭/계보도 상호작용) — Chrome 확장 미연결로 이번 세션에서 스킵됨
  - Notion 프로젝트 페이지 미생성(페이지 자체가 바뀌었으므로 기존 페이지 갱신 또는 새로 생성 필요)

- 재개 시 첫 액션: 음성은 `audio/`에 새 저널리즘 톤 17클립(wav+mp3+manifest.json)으로 전량 교체 완료(2026-09-11). 브라우저에서 `python -m http.server`로 열어 스크롤/카드/계보도/오디오 재생을 확인하는 것이 다음 단계. 문구를 더 고치거나 참조 음성을 재녹음했다면 재렌더링(로컬 `melotts-test\omnivoice-local` 또는 `rhel-storage:/home/jjyoo/record`에서, `--cpu-cores`는 공유 서버에서만 지정):
  ```bash
  python render_tts.py --narration narration.json --out-dir ./out --device cpu \
    --ref-audio ref/narrator.wav \
    --ref-text "안녕하세요. 오늘은 아주 오래된 이야기를 하나 들려드리려고 합니다." \
    --prompt-cache voice_prompt.pt --force --mp3 --cpu-cores 10
  ```
  진행률은 `tqdm` 표시줄로 확인 가능.

- 결정(2026-09-14): 세션 마감. narration.json 최종 상태(10,379자/17개 항목) 확인 완료. RHEL(rhel-storage:/home/jjyoo/record) 재렌더링 명령 사용자에게 재전달, 실행은 사용자 몫으로 대기 중. 브라우저 수동 검증(Chrome 확장 미연결로 대체)도 사용자 대기 중. 신규 코드 수정 없음.

- 결정(2026-09-15): 사용자가 실제 브라우저에서 15장(최종 확대) 화면 스크린샷 제공 — 노선도 확대(70vh)/파도 도착은 정상 작동 확인. 단 한 정류장에 인물이 3명 이상 몰릴 때(OpenAI 3명, Anthropic 3명, TML 2명, 독립 스타트업 5명) 아바타 원(지름 18px)이 14px 간격으로만 벌어져 서로 겹쳐 뭉개지는 실제 버그 발견 → `riderGridOffset()` 함수 신설, 3명씩 한 줄로 접는 2차원 격자 배치(가로/세로 20px 간격)로 교체. 인원수 배지(badge) 위치도 격자 행 수에 비례해 바깥으로 밀어내도록 수정(기존엔 station 라벨과 같은 y좌표에 겹침). narration.json 텍스트는 무관(10,379자/17개 항목 그대로), JS 문법(node --check) 검증 통과.
  - 위험: 새 배치가 실제 브라우저에서 여전히 겹치지 않는지 육안 재확인 필요(스크린샷으로 재확인 권장)

- 결정(2026-09-15): 사용자 요청 "UI가 고전틱하다, MZ스럽게 세련되게" — 톤 "볼드 & 대비감", 범위 "전체 비주얼 리뉴얼"로 확정 후 CSS 전면 개편.
  - 액센트 컬러를 골드(#FFD37E)에서 네온 민트(#22D3C7, --light)와 네온 라일락(#B18CFF, --accent2) 그라디언트로 교체 — 교체 전 기존 세션 방식대로 상대휘도 대비 계산으로 전 챕터 배경(15종) 대비 최소 4.11:1(라일락)/5.72:1(민트) 확보 검증 후 적용.
  - 제목/라벨/버튼용 폰트로 'Space Grotesk'(볼드 지오메트릭 산세리프) 추가 도입 — h1/h2/ch-no/year/kicker/버튼/스탯 숫자에 적용. 본문 문단(.lines/.desc, Gowun Batang 세리프)은 그대로 유지해 "볼드 헤드라인 + 세리프 본문" 대비 구조로 설계(가독성·측정된 --measure 폭 영향 없음).
  - 버튼(#start/#intro button/#player #play) 그라디언트 채움 + 글로우 섀도 + 호버 리프트, 진행바(#bar) 그라디언트, 카드류(.board/.panel/.pcard/.hero-por-photo) 라운드 처리(8~20px), feature-tag를 네온 아웃라인 글래스 칩으로 재설계.
  - narration.json 텍스트는 변경 없음(재추출 결과 10,379자/17항목 동일, 재렌더링 불필요). JS 로직 무변경(node --check 통과), 페이지 레벨 가로 스크롤 없음 재확인.
  - 위험: 스크린샷 기반 육안 검증은 이번에도 인앱 브라우저 렌더링 한계로 불가 — DOM computed-style로 그라디언트·폰트·라운드 반영만 확인. 실제 화면 임팩트는 사용자 확인 필요.

- 결정(2026-09-15): 사용자 지적 "음성 읽는 블럭만 보여주고 진행하면 되는데 장 전체를 다 뿌려두는 건 비효율" — 확인 질문 답변으로 "재생 여부와 무관하게 항상 지금 읽을 분량 + 이미지만 노출"로 확정. Plan Mode로 설계 후 승인받아 구현.
  - 15개 장 전수 감사 결과 모든 장이 이미 최소 1개 시각자료(인물사진/컨텍스트사진/풀블리드) 보유 확인 — 신규 이미지 소싱 없이 구현 가능했음.
  - `.lines`(장 전체 문단 뭉치) → `.chapter-grid`(좌: `.chapter-visual` sticky 이미지, 우: `.chapter-steps`의 `.step` 문장들)로 구조 변경. feature 장(5·10·12, 풀블리드 사진)은 `.chapter-visual`이 비어 그리드 없이 기존 1단 유지.
  - 기존 재생 중에만 작동하던 "현재 문장만 선명/나머지 블러"(`.page.speaking .lines p`)를 상시 규칙(`.step`/`.step.active`)으로 승격 — 비활성 문단은 opacity .14+blur로 훨씬 강하게 죽임(기존 .38). DOM엔 그대로 남아 스크린리더·Ctrl+F 영향 없음.
  - 스크롤로 읽을 때: 기존 챕터 전환에 쓰던 visRatio-최댓값 IntersectionObserver 패턴을 `.step` 단위로 재사용(`stepIO`), 챕터가 `.speaking` 중이면 개입 안 함.
  - 오디오 재생 중: 기존 `wireCaptionSync` 문장 길이 비율 추정 로직은 그대로 두고, 활성 문장이 바뀔 때만 `scrollIntoView`를 추가해 사용자가 직접 스크롤할 필요를 없앰(원 지적사항 직접 해결).
  - `.page.feature::before`를 sticky로 바꾸는 안은 설계 중 재검토 후 기각(absolute→sticky 전환 시 pseudo-element가 flow에 720px 높이를 차지해 본문이 밀리는 회귀 위험 발견) — feature 장은 배경 로직 그대로 유지.
  - 검증: JS 문법(node --check) 통과, narration.json 재추출 텍스트 100% 동일(오디오 재렌더링 불필요), 인앱 브라우저 DOM에서 15개 장 그리드/비주얼/스텝 개수·최초 활성화·wireCaptionSync 활성 전환 전부 확인.
  - 위험: 이 환경은 뷰포트 폭이 0으로 잡혀 데스크톱 sticky(`min-width:820px`)/실제 스크롤 기반 IntersectionObserver 트리거를 육안 검증하지 못함 — 사용자가 실제 브라우저에서 스크롤 진행감·이미지 고정 여부 확인 필요.

- 결정(2026-09-15): 사용자가 실제 브라우저 캡처 제공 — (1) 재생 후 다른 장으로 넘어가면 문장이 전부 흐려진 채 멈추는 버그, (2) 인물 사진이 sticky 칼럼에서 세로로 쌓여 화면 높이를 넘어 잘려 보이는 문제, 2건 확인 및 수정.
  - 버그1 원인: `clearCaptionSync()`가 DOM의 `.step.active`는 지우지만 `stepIO`의 캐시(`stepActive{}`)는 그대로 둬서, 스크롤 관찰자가 "이미 처리했다"고 착각하고 다시 칠하지 않음. `clearCaptionSync()`에서 캐시도 같이 리셋 + 관찰자 콜백에 "캐시가 최신이어도 DOM에 실제 active가 없으면 다시 칠한다" 가드 추가.
  - 버그2 원인: 기존엔 인물 사진(`.hero-por`)이 폭 넓은 1단 레이아웃에서 `flex-wrap`으로 3장이 한 줄에 들어갔으나, 이번 스텝 진행형 개편으로 좌측 36% sticky 칼럼에 들어가면서 200px 사진 3장이 한 줄에 안 들어가 전부 세로로 쌓임 → 칼럼 높이가 뷰포트를 넘어 sticky가 잘려 보임. `.chapter-visual` 안에서만 사진을 104px로 축소(데스크톱 스코프)해 한 줄에 여러 장이 들어가게 하고, 안전장치로 `max-height:calc(100vh - 8rem);overflow-y:auto`도 추가.
  - 인앱 브라우저에서 `resize_window`로 실제 뷰포트(1440×900 데스크톱, 375×812 모바일) 에뮬레이션 후 재검증 — 이전엔 뷰포트 0px 한계로 못 하던 sticky·미디어쿼리 확인이 이번엔 가능했음. 1/6/15장 비주얼 칼럼 높이 156~222px(뷰포트 900px 이내), 사진 3장이 한 줄 배치 확인. 모바일은 sticky 비활성·132px 사진·그리드 1단 정상. 페이지 레벨 가로 스크롤 없음(계보도 내부 스크롤 제외). player/metro 겹침 없음. 버그1 수정도 DOM 시뮬레이션으로 재현→해결 확인.
  - 위험: 사용자가 캡처에서 함께 언급한 화면 상단의 원형 X(닫기) 아이콘 정체는 코드상 대응 요소를 못 찾아 미해결 — 브라우저 자체 UI인지 재확인 요청함, 답변 대기.

- 결정(2026-09-15): 사용자가 "재생바가 뒤 사진을 가린다" 캡처 제공 — 좌표 실측 결과 새 버그가 아니라 `#player`가 `position:fixed`로 항상 화면에 떠 있는 기존 구조적 특성(Spotify/유튜브류 플로팅 플레이어와 동일 — 스크롤 중 어떤 콘텐츠든 그 순간 아래를 지나가면 가려짐)으로 확인. 다만 배경 불투명도가 90%(rgba(16,18,45,.9))라 뒤가 거의 안 비쳐서, 62%(rgba(16,18,45,.62))로 낮춰 가려도 뒤 콘텐츠가 비치도록 완화. 큰 재설계(자동 숨김 등)는 하지 않음 — 필요하면 추가 요청 시 진행.

- 결정(2026-09-15): 사용자 후속 질문 "재생바에 가려지면 뒤 인물 카드를 클릭해서 정보를 볼 수 있냐" — 확인 결과 실제로 불가능했음(중요 발견). `#player`가 사각형 전체로 클릭을 가로채서, 시각적으로 가려진 게 아니라 실제로 그 영역 전체가 클릭 불가 상태였음(`elementFromPoint`로 재생바 중앙을 찍으면 뒤 콘텐츠가 아니라 `#pos`(시간 표시) span이 잡힘).
  - 수정: `#player{pointer-events:none}` + `#player button{pointer-events:auto}` — 실제 버튼(이전/재생/다음) 3개만 클릭을 받고, 라벨·여백 등 나머지 영역은 클릭이 뒤로 통과되도록 변경.
  - 검증: 수정 후 재생바 중앙(라벨 영역)을 `elementFromPoint`로 찍으면 뒤에 있는 `<p>` 요소가 잡힘(통과 확인), 재생 버튼 위치는 여전히 `#play` 버튼이 잡힘(정상 클릭 유지). JS 문법·narration.json 재검증 통과.
  - 남은 한계: 버튼 자체(3개, 각 ~50-60px)에 정확히 겹치는 카드는 여전히 그 부분만 가려짐 — 버튼이 차지하는 면적이 작아 실사용상 영향 미미로 판단, 추가 조치 없음.

- 결정(2026-09-15): 반복된 "재생바가 콘텐츠를 가린다" 피드백에 대해 사용자가 근본 해결책 제안 — "컨트롤바가 노선도 바 안에 들어가면 가림이 줄지 않나?" 채택. 스크롤 중 옅게 하는 임시방편(opacity dim) 대신 구조 자체를 변경.
  - 이전: 재생바(`#player`, `position:fixed`, 독립된 알약)가 노선도 바(`#metroWrap`, 120px) 위에 별도로 떠서 합쳐 최대 ~190px(데스크톱)/~146px(모바일) 콘텐츠를 덮음.
  - 변경: `#player`를 `position:static`으로 바꿔 `#metroWrap`의 자식으로 편입(JS에서 `metroWrap.prepend(player)`), `#metroWrap`을 `flex-direction:column`으로 바꿔 [재생 컨트롤 행(~33px)] + [노선도 SVG(나머지)]를 하나의 통합 바로 병합. 총 높이 156px(데스크톱)/120px(모바일)로 축소 — 뜬 UI가 2개→1개, 덮는 면적도 줄어듦.
  - 부수 정리: `#player.show`/`#player.final`(독립 위치 지정용 규칙) 제거, 모바일 미디어쿼리의 `#player{bottom:...}` 오버라이드 제거. 이전에 추가했던 "스크롤 중 dim" 로직(`.dim` 클래스, scroll 리스너)은 그대로 유지 — 통합 바에도 여전히 유효한 보완책.
  - 검증: 인앱 브라우저에서 `#player`가 실제로 `#metroWrap`의 자식인지, 재생 버튼이 여전히 클릭되는지(`elementFromPoint`), 노선도 SVG 크기(122px, 기존과 동일하게 유지)와 15장 확대(70vh) 시 컨트롤 행은 그대로 33px·SVG만 확장(596px)되는지 데스크톱(1280×900)·모바일(375×812) 둘 다 확인. narration.json/JS 문법 재검증 통과.

## 기록

- Notion: `빛이 된 아이 - 2026-09-10` 페이지 생성 완료
  https://app.notion.com/p/3d7b44a2dd2e8172b748fda9a01f7e0e
